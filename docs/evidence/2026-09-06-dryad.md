# Dryad 도입과 Grove status 확장의 실행 증거

2026-09-06. 후보는 `main`에 순서대로 들어간 커밋 다섯 개다. 각 커밋 메시지에
같은 증거가 있고, 이 문서는 사람이 한 번에 읽는 기록이다. 운영 명세는
[Dryad 스킬](../../skills/dryad/SKILL.md), [Dryad README](../../skills/dryad/README.md),
[overlay 계약](../../skills/grove/references/overlay-contract.md)이 소유한다.

| 커밋 | 바뀐 동작 |
| --- | --- |
| `aa13da6` | Dryad 스킬. `plan` `seat` `report` `status` `finish`. 워크트리 생성·입양, Grove `overlay create/destroy` 호출, 자리 레지스트리와 저널 |
| `1b496b3` | Grove `overlay status`가 pending 항목에 `in-flight pid <n>` / `stalled` / `unknown` 라벨을 붙임. 종료 코드는 그대로 |
| `98ad14e` | Dryad `status`가 다른 자리의 in-flight mutation을 진행으로 셈. 두 자리 동시 attach 측정 |
| `bf982f5` | Grove `overlay status --json`. drift와 liveness를 데이터로 만들고 텍스트는 거기서 포맷 |
| `a18d149` | Dryad가 텍스트 정규식 대신 `--json`을 읽음 |

## 실행한 것

기존 소비 프로젝트와 공유 엔진은 사용하지 않았다. 임시 Git 저장소를 baseline으로,
실제 `git worktree`를 자리로, Grove의 격리 process 백엔드(`infra/bin/fixtures/process-overlay.mjs`)를
overlay 명령으로 썼다. 레지스트리는 `GROVE_STATE_DIR` 아래 임시 디렉터리다.

```bash
npm test
node --test infra/bin/dryad.test.mjs
node --test infra/bin/overlay.test.mjs
node --test infra/bin/overlay-parallel.test.mjs
```

## 센 결과

| 검사 | 결과 |
| --- | --- |
| `npm test` (a18d149 기준) | 192 pass / 0 fail. Dryad 도입 전 182 |
| Dryad 파서: 허용 키, `{id}` 외 자리표시자, 빈 root 거부 | 7/7 거부 |
| plan `--apply` 없이 | 워크트리 0, 레지스트리 파일 없음 |
| plan 생성 경로 | 워크트리 1/1, 브랜치 `dryad/w1`, base = baseline HEAD, baseline git status 깨끗 |
| `seat --shell`을 실제 `sh -c`로 실행 | 프로브 프로세스의 cwd가 워크트리, `DRYAD_*` 넷 도달 |
| report 3회 | status 갱신 3/3, 저널 3줄, session 저장 |
| finish 더러운 워크트리 | 거부 1/1, 워크트리와 저널 유지 |
| finish 깨끗한 워크트리 | 워크트리 제거 1/1, 브랜치 유지 1/1, 레지스트리 파일 삭제 |
| 입양: baseline 자신, 다른 저장소, worktrees 절 없음 | 거부 3/3. 입양 후 finish는 워크트리 유지 1/1 |
| overlay create 실패 → pending → 같은 plan 재실행 | pending 1/1, 재시도 확정 1/1, Grove 레지스트리에 env 존재 |
| 두 자리 동시 plan | 워크트리 2/2, 자리 2/2 |
| 두 자리 동시 attach (w1을 게이트로 붙잡은 채 w2 attach) | 붙잡힌 동안 `envs 2/2 tracked, 1 in-flight`, problems 0. 풀린 뒤 Grove 레지스트리에 두 이미지 2/2 |
| 중단 주입 후 attach | `pending stalled` 문제 1/1, 재실행으로 복구 1/1 |
| Grove `status` liveness 라벨 | 게이트로 붙잡힌 attach에서 `in-flight pid` 1/1, 중단 후 `stalled` 1/1, 종료 코드 두 경우 모두 1 |
| Grove `status --json` | 깨끗한 보고와 stalled 보고 2/2, 텍스트와 종료 코드 일치 1/1, status 외 동사에서 `--json` 거부 1/1 |

## 가드를 되돌린 실행

각각 프로덕션 변경만 되돌리고 테스트를 돌린 뒤 복구했다.

| 되돌린 가드 | red |
| --- | --- |
| Dryad unknown key 거부, 더러운 워크트리 거부, baseline 입양 거부 | 3 |
| Grove liveness 라벨 | 2 (Grove 1, Dryad 1) |
| Dryad in-flight 허용 | 1 |
| Grove JSON 분기 | 4 (Grove 2, Dryad 2) |

## 실제 에이전트를 자리에 앉힌 수동 실행

같은 날 사람이 한 번 실행했다. 자동 검사가 아니며, 런처는 설계상 Dryad 밖이므로
Dryad의 회귀 검사에는 들어가지 않는다. 이 기록은 "자리 계약이 실제 도구에
그대로 먹히는가"에 대한 한 번의 관측이다.

준비: 임시 저장소 하나에 `runtime-profile.yml`(`overlay: none`)과
`dryad-profile.yml`(`worktrees` 절 없음, 전부 입양), `.agents/skills/dryad` 심링크,
짧은 `AGENTS.md`. 데스크톱 워크트리 관리 앱에 이 저장소를 등록하고, 앱이
워크트리를 만들게 한 뒤 Dryad가 입양했다. 자리는 앱이 연 셸에 `seat --shell`
한 줄을 보내고 그 뒤에 도구를 붙이는 방식으로 넘겼다.

```bash
de-novo skills dryad plan w1 --task-file ../w1-task.md --worktree <app-made-path> --by claude --apply
# app shell:  eval "$(de-novo skills dryad seat w1 --shell --project <baseline>)" && claude --dangerously-skip-permissions "$(cat ../w1-task.md)"
de-novo skills dryad plan w2 --task-file ../w2-task.md --worktree <app-made-path> --by codex --apply
# app shell:  eval "$(de-novo skills dryad seat w2 --shell --project <baseline>)" && codex --dangerously-bypass-approvals-and-sandbox "$(cat ../w2-task.md)"
de-novo skills dryad status
de-novo skills dryad finish w1 --apply && de-novo skills dryad finish w2 --apply
```

작업 본문은 여섯 줄이다. 환경변수를 echo하고 스킬을 읽고, `seat --json`으로 자기
브랜치를 확인하고, NOTES.md에 한 줄을 붙여 그 파일만 커밋하고, `report done`을
치고, 커밋 해시를 답한 뒤 멈춘다. 푸시와 finish는 금지했다.

| 관측 | w1 (Claude Code) | w2 (Codex) |
| --- | --- | --- |
| `DRYAD_ID` `DRYAD_PROJECT` 도달 | 도구가 echo로 출력 | 도구가 echo로 출력 |
| 스킬 읽음 | 읽음 | 읽음(+79줄 출력) |
| `seat --json`으로 브랜치 확인 | w1 | w2 |
| 커밋 | `d26d654 w1: note`, NOTES.md만 | `4623dfe w2: note`, NOTES.md만 |
| `report done` 자기 보고 | 저널에 `seat report done: NOTES.md updated` | 같음 |
| 푸시·finish 안 함 | 지킴 | 지킴 |
| `dryad status` | `reported done 2`, `+1` 둘, exit 0 | |
| `finish --apply` | 입양 워크트리 유지, 브랜치 유지, 자리 제거 2/2 | |

사람이 개입한 곳 둘. 두 도구 모두 처음 여는 디렉터리에 신뢰 확인을 띄웠고,
사람이 앱 터미널에 키를 보내 답했다. 이건 런처의 일이고 Dryad는 모른다. 앱의
`tui-idle` 대기는 Codex가 아직 작업 중일 때 한 번 조용한 구간을 유휴로 판정했다.
완료 판정을 프로세스나 화면이 아니라 작업자의 `report done`으로 두기로 한 결정이
여기서 실제로 값을 했다. Dryad `status`를 폴링해 `done`을 기다리는 것으로 충분했다.

두 도구는 자리에서 전권 플래그로 실행했다. 임시 저장소였기 때문이며, 어떤
권한 정책으로 앉힐지는 런처와 사람의 값이다.

## 실제 프로젝트에서 overlay까지: 이름만으로 두 자리

같은 날 저녁, 실제 브랜드 사이트 저장소(TanStack Start, Vite dev 서버)에서 실행했다.
먼저 `overlay: none`으로 자리 다섯 개(콘텐츠, 라우트, 캡처, 전후 그래픽 둘)를
돌렸고, 그때는 자리마다 dev 서버 포트를 지시서에 손으로 나눠야 했다. 그것이
Grove를 안 쓴 것임을 확인한 뒤, 프로젝트에 overlay 어댑터를 심고 두 자리를 다시
돌렸다.

프로젝트가 소유한 것: `tools/dev-overlay.mjs`(Grove overlay 계약의 process
backend. `attach <env> web --image de-novo-me/web:<sha> -- --worktree <path>`가
그 워크트리의 Vite를 빈 loopback 포트에 띄우고 기록한다. status는 실제 프로세스와
HTTP 200을 관측한다. 이미지 sha가 워크트리 HEAD와 다르면 거부한다),
`tools/dev-router.mjs`(`web--<env>.de-novo-me.localhost` → 기록된 포트,
`web.de-novo-me.localhost` → baseline :3000. 8080에서 듣는다. `proxy: project`),
프로파일의 `overlay.attachable: [web]`, `runtime.commands.overlay`.

Grove 경로를 사람이 먼저 한 바퀴 돌렸다.

| 단계 | 관측 |
| --- | --- |
| `overlay create t1 --apply` | postcondition 1/1 |
| `overlay attach t1 web --image …<sha> --apply -- --worktree <w5>` | postcondition 1/1 (status 6회 관측 뒤 ready), upstream `127.0.0.1:61158` |
| `overlay status` | environments 1, attachments 1, drift 0, exit 0 |
| `http://web--t1.de-novo-me.localhost:8080/work/de-novo-skills` | 200, w5 브랜치에만 있는 내용을 서빙 |
| `http://web.de-novo-me.localhost:8080/` | 처음 502(라우터가 127.0.0.1로 붙었는데 baseline은 ::1에만 있었다) → localhost로 고친 뒤 200 |
| `overlay destroy t1 --apply` | postcondition 1/1, Vite 프로세스 0, state 비움 |

그 다음 자리 둘. w6(Claude Code)은 `/work` 색인 시트, w7(Codex)은 헤더의 조건부
`WORK` 링크. 지시서에는 포트가 한 줄도 없다. 작업자는 커밋 → HEAD sha로
`overlay attach` → 자기 이름으로 curl → `overlay status` exit 0 → `report done
--session`.

| 관측 | w6 Claude Code | w7 Codex |
| --- | --- | --- |
| `overlay create` (plan 시 Grove가 실행) | 1/1 | 1/1 |
| `overlay attach` postcondition | 1/1, 이미지 sha = 워크트리 HEAD | 1/1, 이미지 sha = 워크트리 HEAD |
| 자기 이름으로 확인 | `/work` 200, `/work/does-not-exist` 404 | `/` 200, 헤더 `href="/work"` 정확히 1 |
| 격리 | w6 홈에는 헤더 링크 0 | w7 `/work`는 404 |
| baseline 이름 | `href="/work"` 0, 200 (두 자리의 변경이 새지 않음) | |
| `report --session` | 남김 | 남김 |
| `finish --apply` | env 1/1 destroyed, 워크트리·브랜치 유지 | 같음 |
| 종료 후 | Grove environments 0, 어댑터 Vite 프로세스 0, 라우터 내림, baseline :3000 그대로 | |

사람이 개입한 곳: 어댑터와 라우터 작성, 라우터의 baseline 주소 수정 한 번. 신뢰
확인은 이번엔 뜨지 않았다(같은 저장소 root를 이미 신뢰). 두 도구 모두 `DRYAD_SKILL`로
스킬을 읽었고 `seat --task`로 첫 프롬프트를 받았다.

## 남은 경계를 재다 (같은 날 밤)

"재지 않은 것" 목록을 하나씩 실제 경계에서 실행했다. 카탈로그 후보는 `da3ab6a`
이후 커밋들이다.

### 교체와 중단 복구, 실제 어댑터로

브랜드 사이트 저장소의 임시 워크트리를 자리 p1로 입양하고 process 어댑터로 밟았다.

| 단계 | 관측 |
| --- | --- |
| attach A | postcondition 1/1 (status 12회 관측), 이름으로 200 |
| 커밋 B 뒤 attach B | postcondition 1/1 (5회). 옛 pid 죽음, 새 pid 살아 있음 |
| 커밋 C 뒤 attach C, 검증 기한 1ms | pending 유지, exit 0이 아님 |
| 그 상태의 보고 | Grove `pending-item attach p1/web stalled`. Dryad `problem overlay pending stalled` 와 그 env의 drift 문제 |
| 같은 attach C 재실행 | postcondition 1/1 (3회), pending 0 |
| finish p1 | env 1/1 destroyed, 어댑터 Vite 프로세스 0 |

### 스크립트 작업자 하네스 (회귀 검사에 들어감)

`infra/bin/fixtures/dryad-worker.mjs`는 실제 도구 없이 앉은 작업자처럼 행동한다.
런처가 하는 그대로 `seat --shell` 한 줄 뒤에 프로그램을 실행하고(cwd는 일부러
워크트리가 아닌 곳), `DRYAD_*`만으로 자리·스킬·작업을 읽고, working을 보고하고,
파일 하나를 바꿔 자기 브랜치에 커밋하고, session과 함께 done을 보고한다. 테스트가
세는 것: 자리·스킬·작업 읽기 3/3, 자기 브랜치 커밋 1/1, 워크트리 깨끗·baseline
그대로, session 있는 done 1/1, status의 done 1과 +1. 실제 에이전트 도구는 여전히
수동 실행 셋뿐이며 설계상 회귀 검사에 넣지 않는다.

### 공유 엔진: 데이터 격리, 그리고 사고 하나

임시 프로파일 둘(`dryad-proof-a`, `dryad-proof-b`, 각각 pg 데이터베이스 하나와 redis
prefix)에 `setup`을 실행했다. 엔진은 이미 돌고 있었다.

| 관측 | 결과 |
| --- | --- |
| `setup` a, b | engines 2/2, DBs 1/1 씩 |
| a가 자기 DB에 테이블·행 생성 | 1 |
| b가 a의 DB에 접속 | 거부: CONNECT 권한 없음 |
| b가 a의 테이블 select / create / drop | 전부 거부 (접속 단계에서) |
| a의 데이터 | 그대로 |
| 정리 | 두 DB와 role drop, 잔여 0 |

**사고.** 같은 실행에서 `setup`이 돌고 있던 `dev-pg16`과 `dev-redis7`를 재생성했다.
컨테이너 ID가 바뀌었고, 데이터는 named volume이라 남았으며, 선언하지 않은
`dev-mysql8`은 그대로였다. compose 파일의 config hash가 엔진 기동 시점과 달라져
`up -d --wait`가 재생성한 것이다. infra README가 경고해 둔 경우가 실제로 일어났다.
같은 밤 `setup`과 `infra up`에 `--no-recreate`를 넣었다(`7040c93`). docker shim으로
두 진입점의 compose 인자를 세는 테스트 2/2, 가드를 빼면 red 1, 그리고 공유 엔진에
플래그를 붙여 `setup`을 다시 실행해 컨테이너 ID가 그대로인 것 2/2. 플래그가 실제
config drift 아래에서 재생성을 막는지는 공유 엔진에 drift를 일부러 만들 수 없어
compose 문서의 의미에 기댄다.

### Kubernetes 실험실 재실행

현재 CLI(Grove status의 liveness 라벨, `--json`, drift 객체가 들어간 뒤)로 실험실을
다시 돌렸다. 12/12 케이스 통과, 비공개 클러스터 제거, 임시 워크트리 0. 첫 실행의
Preservation check는 실패했는데 원인은 위 `setup` 사고가 같은 시간에 기존
컨테이너 ID를 바꿨기 때문이다. 아무것도 건드리지 않고 한 번 더 돌린 결과는 아래
표에 있다.

| 항목 | 첫 실행 (후보 `da3ab6a`) | 재실행 (후보 `7040c93`) |
| --- | --- | --- |
| 케이스 | 12/12 통과 | 12/12 통과 |
| 실패 항목 | Preservation check 1 (위 사고 탓) | 0 |
| 기존 컨테이너 보존 | 측정 불가(사고와 겹침) | 14/14 |
| 기본 kubeconfig | 그대로 | 그대로 |
| 정리 | 클러스터 제거, 임시 워크트리 0 | 클러스터 제거, 임시 루트 제거, 임시 워크트리 0 |
| 소요 | 6분 34초 | 6분 23초 |

원시 보고서는 로컬 경로를 담고 있어 저장소에 넣지 않았다. 케이스 목록은
[실험실 README](../evaluation/kubernetes/README.md)와 같다.

### 다음 날: 두 개선과 시각화 자리 둘 (2026-09-07)

overlay 자리 두 개를 앉히며 드러난 마찰 둘을 Grove에 넣고(`8ec6389`) 실제 어댑터로
쟀다.

- **`GROVE_CALLER_CWD`.** 작업자가 attach마다 `-- --worktree "$PWD"`를 넘겨야 했다.
  Grove가 호출자 디렉터리를 프로젝트 명령의 환경변수로 넘기고, 어댑터가 그것을
  기본값으로 쓴다(검증은 어댑터 몫). 측정: 자리 워크트리에서 플래그 없이 attach →
  기록된 worktree가 그 경로, 이름으로 200. baseline 디렉터리에서 attach → 거부.
- **거부 영수증.** 어댑터가 검증 단계에서 거부하면(exit 1) Grove가 dispatch 전에 쓴
  pending이 남아 같은 attach가 성공할 때까지 env가 잠겼다(destroy 불가). 실제로
  한 번 잠겼고, 워크트리를 같은 경로에 다시 만들어 같은 attach를 성공시켜 풀었다.
  이제 `{ok:false, mutated:false}` 마지막 줄이면 Grove가 그 dispatch의 pending을
  거둔다. 이전 pending의 재시도가 거부되면 그 pending은 남는다. 측정: 거부 뒤
  pending 0, finish 막히지 않음. stub 테스트 2/2, 가드 red 각 1.

그 뒤 사이트의 스킬 소개 시트에 자리 둘. 지시서에 포트도 `--worktree`도 없다.

| 관측 | w8 Claude Code (아스키 그림) | w9 Codex (스크롤 모션) |
| --- | --- | --- |
| 결과물 | `AsciiFigure`(타자로 쳐지는 `<pre>`, SSR은 완성 상태, reduced motion 정적, 좁은 화면은 가로 스크롤), FIG. 02 Grove·03 Overlay·04 Dryad를 Approach 문장 뒤에 배치 | 읽기 진행 막대, 현재 절을 따라가는 sticky 러닝헤드, 절별 once 리빌, Approach를 렌더 시 문장 단위로 분리, Outcome 숫자 카운트업 |
| 커밋 | 3 (apps/web만) | 2 (apps/web만) |
| attach·이름 확인 | postcondition 1/1, SSR에 plate 3, 390px에서 plate만 스크롤 | postcondition 1/1, 러닝헤드 5 상태, 문장 10개 정확, 320px overflow 0, layout shift 0 |
| session | 남김 | 남김 |
| 소요 | 36분 (커서 hand-off 수정으로 재attach 2회) | 10분 |

병합은 두 자리가 같은 라우트 파일의 Approach 렌더를 고쳐 충돌했다. 사람이 w8의
그림 삽입을 w9의 문장 리빌 컴포넌트 안으로 접어 넣어 풀었다(그림은 앵커 문장
바로 뒤, 앵커가 사라지면 절 끝). 합친 main(`4f2e7ac`)을 띄워 `<pre>` 3,
`<figure>` 4, 문장 prose 1, 색인 200, 미존재 404를 세고 Playwright로 라이트·다크
각 5장을 찍었다. 페이지 에러 0.

관찰 하나. w8의 Claude Code는 전권 플래그로 떠 있었는데도 `rm "$OUT"/*.png`에
"Dangerous rm operation" 확인을 띄웠다. 사람은 답하지 않았고 작업자가 스스로
넘어갔다(어떻게 넘어갔는지는 화면에 남지 않았다). 런처 쪽의 마찰이며 Dryad는
모른다.

### 도그푸딩: 카탈로그 자체에 자리를 놓고 캐노피를 만들다 (2026-09-07)

카탈로그에 Dryad를 심으려다 "Grove 없는 프로젝트" 경로가 구현에 없던 것을 발견해
고쳤다(`aeb9c6d`). 그 뒤 두 저장소에 자리 셋을 동시에 열었다. 카탈로그의 자리는
워크트리와 보고뿐이고, 사이트의 자리는 overlay env를 받는다.

| 자리 | 저장소 · 도구 | 결과물 | 보고한 측정 |
| --- | --- | --- | --- |
| c1 | 카탈로그 · Claude Code | plan이 쓰는 프로젝트 색인 `projects.yml`, `dryad projects [--json]`, `urls --json`, `status --json`의 자리별 `hostnames` | npm test 201/201, 가드 9개 되돌려 9/9 red |
| c2 | 카탈로그 · Codex | `de-novo skills canopy [--port] [--once]`: 공개 CLI의 JSON만 읽는 127.0.0.1 전용 읽기 페이지(SSR + 5초 폴링, JS 없이도 렌더) | npm test 204/204, canopy 테스트 7/7(실제 임시 저장소·소켓·process backend), 가드 9/9 red |
| m1 | 사이트 · Claude Code | FIG. 05 타자 터미널 트랜스크립트(plan, seat --shell, status, finish) | 이름으로 200, SSR에 FIG. 02–05, layout shift 0, 캡처 5 |

c2는 c1의 `projects --json` 형태를 지시서로만 알고 fixture에 대고 만들었다. 합친
뒤(`5e38159`) 전체 208/208. 색인은 plan 시점에 쓰이므로 두 저장소에 probe 자리를
하나씩 만들어 채웠고, `dryad projects`가 2개 프로젝트를 셌다. 캐노피를 실제 머신
상태로 띄워 라이트·다크로 찍었다. 그 화면에는 사이트 probe의 attach가 `stalled`
pending과 drift로 잡힌 문제 행이 실제로 떠 있다. 원인은 사람이 워크트리의
node_modules를 심링크로 때워 pnpm이 깨진 것이고, 진짜 설치 뒤 같은 attach를
재실행해 확정됐다(attempts 13). Grove·Dryad·캐노피 셋 다 그 사이 상태를 정확히
보였다.

병합: c1과 c2가 같은 `cli.mjs`와 README를 고쳤지만 자동 병합됐다. 세 자리 모두
apps/web 또는 infra 밖 변경 0, 푸시 0, session 남김 3/3.

### 잴 수 없는 것과 재지 않을 것

- **:80 리스너.** macOS에서 root가 필요하고 이 머신의 `sudo`는 비밀번호를 요구한다.
  라우터는 8080에 듣는다. "포트를 기억하지 않는다"는 머신에 하나 남은 포트까지
  없애야 완성이며, 그것은 리스너를 root로 두는 운영 결정이지 Grove 코드가 아니다.
- **생산성 효과.** 시간 절감이나 충돌 감소는 이 저장소가 주장하지 않는 것이고,
  하루의 실행으로 잴 수 있는 것도 아니다. 제품 방향 문서가 같은 말을 한다.
- **실제 에이전트 도구의 회귀 검사.** 자격증명과 도구가 필요하고 런처는 설계상
  밖이다. 위 하네스가 계약 쪽을, 수동 실행 셋이 도구 쪽을 맡는다.
- 공유 백엔드(k3d, compose)의 adapter 동시 실행. 이전과 같은 경계.
- 다른 호스트가 잡은 락의 liveness. 레지스트리가 머신 로컬이라 `unknown`으로만
  표시한다.
