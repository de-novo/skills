# Seats for the playground sample project. The slug lives in the Grove
# profile next to this file, so it is not repeated here.
version: 1
worktrees:
  root: ../seats              # <sandbox>/seats, beside <sandbox>/project
  branch: "seat/{id}"
