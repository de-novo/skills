# Clone-local values — not the catalog. Includes infra/addressing.local.yml
# and a project's .agents/runtime-profile.local.yml.
*.local.md
*.local.yml

node_modules/
.playground/
.playground-evidence/

# Tool workspace pointers — clone-local, never the catalog.
.haru.json
